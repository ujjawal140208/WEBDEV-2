// API: Application programming interface
var a = 10;
console.log(a);
function demo(){
    let b = 20;
    console.log(b);
}
demo();
// callstack: 
// it is a process and mechanism which keeps track of every process running in the background or js codes 
// it stores global execution context and function execution context
// after the processing of the codes it will pop out the FEC and then GEC at the end.